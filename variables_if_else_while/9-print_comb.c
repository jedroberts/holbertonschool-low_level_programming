#include <stdio.h>

/**
 * main - Prints all possible combinations of single numbers
 *
 * Return: awlays 0 (Success)
 */
int main(void)
{
	int n;

	for (n = '0'; n <= '9'; n++)
	{
		putchar(n);
	}
	if (n = '0'; n <= '8'; n++)
	{
		putchar(',');
		putchar(' ');
	}
	putchar('\n');
	return (0);
}
